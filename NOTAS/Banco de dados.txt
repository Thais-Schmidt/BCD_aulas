CREATE DATABASE TESTE;   -> CRIA UM BANCO DE DADOS CHAMADO LOJA

SHOW DATABASES; -> exibe os bancos de dados existentes

DROP DATABASE TESTE; -> exclui o banco de dados

USE TESTE;

CREATE TABLE TBL_CLIENTES(
ID INT NOT NULL AUTO_INCREMENT,
NOME VARCHAR (45) NOT NULL,
TELEFONE VARCHAR(45) NOT NULL,
ENDERECO VARCHAR(100),
PRIMARY KEY (ID)
);

DESC TBL_CLIENTES;

ALTER TABLE TBL_CLIENTES
DROP COLUMN ENDERECO;

INSERT INTO TBL_CLIENTES(NOME, TELEFONE) 
VALUES('THATA', '19 974111188');

TRUNCATE TBL_CLIENTES; -> DELETA TUDO

SHOW TABLES;

DESC TBL_CLIENTES;

SELECT * FROM TBL_CLIENTES; -> MOSTRA A TABELA COM TODAS AS INFORMAÇÕES

TRUNCATE TABLE TBL_CLIENTES;

DROP TABLE TBL_CLIENTES ;

SELECT * FROM LOJA.TBL_CLIENTES - TODA A TABELA
SELECT NOME FROM LOJA.TBL_CLIENTES - APENAS OS NOMES
SELECT NOME, ENDERECO FROM LOJA.TBL_CLIENTES - NOMES E ENDEREÇOS

ALTER TABLE TBL_CLIENTES
ADD COLUMN ENDERECO VARCHAR(50);

ALTER TABLE TBL_CLIENTES
ADD SOBRENOME VARCHAR(15)
AFTER NOME;

DELETE FROM TBL_CLIENTES WHERE ID=1; -> DELETA APENAS A LINHA DO ID 1

DELETE FROM TBL_CLIENTES WHERE ID IN (4,5,6,7); -> DELETA AS LINHAS DOS IDS DE DENTRO DO PARENTESES

CREATE TABLE IF NOT EXISTS TBL_TELEFONES ( 
ID INT NOT NULL AUTO_INCREMENT,
ID_CLIENTE INT NOT NULL ,
NUMERO VARCHAR(11) NOT NULL,
PRIMARY KEY (ID),
FOREIGN KEY(ID_CLIENTE) REFERENCES TBL_CLIENTES(ID)
);

SELECT * FROM TBL_TELEFONES;

ALTER TABLE TBL_CLIENTES 
MODIFY PESO DOUBLE; -> ALTEROU O TIPO DO DADO

select nome as cliente, sexo from tbl_clientes; -> mostra apenas as colunas selecionadas
--------------

NÃO DA PRA EXCLUIR UM CLIENTE EM UMA TABELA COM CHAVE PRIMARIA, O BANCO NÃO DEIXA PELO BEM DA INTEGRIDADE


INSERT INTO TBL_CLIENTES(NOME, SOBRENOME, SEXO, PESO, DATA_NASC) 
VALUES('THAIS', 'SCHMIDT', 'F', 76 , '2001-05-23');


SELECT * FROM TBL_CLIENTES; 

INSERT INTO TBL_TELEFONES(ID_CLIENTE, NUMERO) 
VALUES('9', '19974111188');

SELECT * FROM TBL_TELEFONES;


--------------

SELECT *   (selecionando tudo)
FROM TBL_CLIENTES AS CLI ( da tabela clientes, e apelidando ela de cli)
INNER JOIN TBL_TELEFONES AS TEL (e apelidando a tabela telefone de tel)
ON CLI.ID=TEL.ID_CLIENTE; (unido o id da tabela cliente com o id_cliente da tabela telefone)

-------------------------

TABELA NO EXCEL MAIS ROBUSTA (COM MAIS CAMPOS)
NORMALIZAÇÃO DE DADOS - CRIAR O DIAGRAMA
SCRIPT DAS TABELAS QUE O DIAGRAMA RESULTAR
